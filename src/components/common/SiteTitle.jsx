import React from "react";
import { SiteTitleDiv } from "../../layouts/styles";

const SiteTitle = ({ title }) => (
  <SiteTitleDiv>
    <h3>{title}</h3>
  </SiteTitleDiv>
);

export default SiteTitle;
